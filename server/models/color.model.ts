export default class Color{
    public id : string;
    public value : string;
    public name : string


  constructor(id: string, value: string, name: string) {
    this.id = id
    this.value = value
    this.name = name
  }
    
}